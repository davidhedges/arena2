# Spell VFX Cue Migration Map — living status board

**What:** classifies every SPELL ability by how its authored `combat_vfx_cues` compare to what the
generator (`SpellVfxGenerator`) would produce, so we can migrate the corpus in a data-driven order.
**How:** generated by running the **real** generator core (`Wire`/`DeriveArchetype`/`RequestedSlots`)
against the live catalog, mirroring the authoring window's facts/school/slot-inference derivation.
Regenerate whenever cues or the generator change. **First generated 2026-07-08.** Companion to
`docs/spell-presentation-dry-redesign-2026-07-07.md`. See [[project_spell_presentation_dry_redesign]].

**Legend:** "clean wiring" = the generator reproduces the authored `trigger/anchor/attach/role/lifecycle`
for every slot 1:1, so migration is just adopting the authored `vfx_id`/`duration` into the palette. It
does **not** mean the *visuals* are right (a drifted spell can be wiring-clean).

## Summary (47 spells)

| bucket | count | what it needs |
|---|---|---|
| **Clean — zero-diff-ready** | 8 | palette-structure decision (which `vfx_id`s are school-generic vs per-spell signature); then the 1:1 writer (done) materializes them |
| **Generator adds a slot** | 8 | writer **insertion** (not yet built — the 1:1 writer refuses these) + a palette entry for the added slot |
| **Catalog-only slot** | 6 | a slot-inference / archetype nuance (deferred SelfNova burst vs impact) to resolve |
| **Wiring diff** | 7 | a real finding to adjudicate (generator rule vs authoring) — some are known/expected |
| **No cues authored** | 18 | palette + prefab + writer insertion (mostly auras/buffs) |
| **No archetype** | 0 | — |

## Clean — zero-diff-ready (8)

`SPELL_FIREBALL` (done), `SPELL_ICICLE` (COLD/charged), `SPELL_GLACIAL_SPIKE` (COLD/charged TargetHit),
`SPELL_FROST_NOVA` (COLD SelfNova), `SPELL_LIGHTNING` (LIGHTNING GroundAoe), `SPELL_ORBITING_BLADES`
(ARCANE Projectile), `WARRIOR_INTIMIDATE` (SelfNova, no school), `WARRIOR_SHOCKWAVE` (SelfNova, no school).

- These migrate with **no wiring change** once the palette supplies their authored `vfx_id`s.
- **Content decision per spell:** is each `vfx_id` school-generic (→ school palette) or per-spell
  signature (→ signature override)? e.g. `VFX_ICE_CAST_HAND_01` looks COLD-generic; `VFX_ICICLE_PROJECTILE_01`
  is ICICLE-signature.
- **`SPELL_ORBITING_BLADES` is wiring-clean but visually drifted** — it's ARCANE yet wears
  `VFX_ICE_CAST_HAND_01` / `VFX_ICE_HIT_01` (design §2.2). Adopting its authored ids verbatim keeps the
  drift; a school palette would *correct* it to arcane visuals (a deliberate behavior change, not zero-diff).
  Decide keep-as-signature vs correct.

## Generator adds a slot — needs writer insertion + palette (8)

| spell | adds | note |
|---|---|---|
| `SPELL_METEOR` | CastGlow | charged sky-drop shows a hand glow while charging |
| `SPELL_MAGIC_MISSILE` | Impact | channel-projectile; impact not authored |
| `SPELL_FROZEN_SPLINTERS` | CastGlow | channel-projectile |
| `SPELL_INSTANT_BEAM` | CastGlow | charged beam |
| `SPELL_BOOMERANG_ORB` | CastGlow | SHADOW projectile |
| `SPELL_WITHERING_ORB` | CastGlow | SHADOW projectile |
| `PALADIN_BLESSED_SHIELD` | CastGlow, Impact | only `projectile_body` authored |
| `PALADIN_BLADE_BARRIER` | CastGlow, Impact | only `projectile_body` authored; also authored `RIGHT_HAND` (generator defaults LEFT — E7) |

These are the concrete justification for the **writer insertion path**: the 1:1 update writer can't add a
row that has no authored counterpart.

## Catalog-only slot — inference/archetype nuance (6)

`SPELL_ICE_SPIKES`, `SPELL_FROZEN_GRASP` (COLD SelfNova, deferred): the generator emits a **Burst** at
`AREA_IMPACT`/`AREA_ORIGIN`, but the authored cue infers to the **Impact** slot (same wiring, different
slot name) because §3.4 keys `Burst` on a *caster* anchor and a deferred burst lands on `AREA_ORIGIN`
(an impact-class anchor). `SPELL_ICE_SPIKES` additionally has two authored rows inferring to one slot
(the ABILITY + SPELL-fallback pair) → `ambiguous`. `PALADIN_WARDING_AURA`, `SPELL_BLINDING_LIGHT`,
`WARRIOR_BATTLE_CRY`, `SPELL_NEGATE`: generator's requested slot (AuraGround/SelfFlash/Impact) vs an
authored Burst. **Action:** extend the §3.4 inference so a deferred-area one-shot at `AREA_ORIGIN` keys to
the archetype's own slot (Burst for SelfNova, Impact for GroundAoe), or have those archetypes agree on one
slot name.

## Wiring diff — findings to adjudicate (7)

| spell | diff | likely verdict |
|---|---|---|
| `SPELL_CONSECRATE`, `SPELL_ERUPTION`, `SPELL_FROST_NEEDLE` | generator `AREA_IMPACT`@`AREA_ORIGIN` vs authored `SPELL_RELEASE`@`IMPACT_POINT` | generator thinks these are **deferred** (impact_delay / cone) but they were authored as immediate. Reconcile: is the deferral real (fix authoring) or spurious (tighten `deferred` detection)? `SPELL_LIGHTNING`, same archetype but non-deferred, is clean. |
| `PALADIN_SACRED_FLAME` | generator `TARGET` anchor vs authored `IMPACT_POINT` | TargetHit anchors on TARGET (Rule 15 legal on SPELL_IMPACT); authoring used IMPACT_POINT. Decide the archetype default. |
| `PALADIN_CLEANSING_TOUCH` | generator `SPAWN_WORLD` vs authored `FOLLOW_ANCHOR` | a target-attached cleanse; should TargetHit impact follow the target? |
| `SPELL_ELECTROCUTE` | generator `UNTIL_CAST_END` vs authored `UNTIL_TERMINAL_EVENT` | **expected** — parked legacy relic (decision 9), off-palette by design. |
| `WARRIOR_GROUND_SLASH` | generator `projectile_body` `LEFT_HAND` vs authored `CASTER` | **expected** — known per-spell body-origin override (design B.3). |

## No cues authored — palette + prefab + insertion (18)

Mostly auras + self-buffs: 6 Paladin auras (`FERVOR`, `THORNS_AURA`, `AURA_OF_VENGEANCE`, `MANA_FONT`,
`STAMINA_FONT`, plus `WARDING_AURA` in the catalog-only bucket), 9 Warrior `APPLY_STATUS`/`SELF_RESOURCE`
buffs, `PALADIN_ABSOLUTION`/`WARRIOR_FEAST` (TargetHit), `WARRIOR_IRON_WILL`/`PALADIN_SERRATED_BLADES`.
All need a palette + a registered prefab and (being cue-less today) pure insertion. Auras are the DRYest
archetype — most want only a brief `aura_ground` flourish.

## Recommended sequencing

1. **Writer insertion path** — ✅ **done** (`SpellCueCatalogWriter` now inserts generator-only slots and
   brand-new owners in sort order, keeps unmatched authored rows, and stays byte-identical on update-only
   owners; verified live + EditMode tests). Remaining: relax the authoring window's write gate (currently
   1:1-only) to *use* insertion, which needs a sort_order-assignment policy for the added slots.
2. **Resolve the deferred-area findings** (CONSECRATE/ERUPTION/FROST_NEEDLE + the Burst/Impact slot nuance)
   — a small generator/inference reconciliation that also clears 4 of the "catalog-only"/"wiring-diff" rows.
3. **Palette content pass** (owner) — decide school-generic vs signature for the clean-8's `vfx_id`s;
   correct the `ORBITING_BLADES` drift; then the 1:1 writer materializes the clean-8.
4. **Externalize palettes into the per-school VFX-set asset** (decision 10) once the content above exists to
   justify the asset shape.
