[package]
name = "arena-hub-v2-rehearsal"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[features]
default = []
pvp_match = []

[dependencies]
log = "0.4"
spacetimedb = "=2.0.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
