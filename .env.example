# Copy to .env and fill in local secrets.
# .env is ignored by git.

# Required for Hetzner resources.
HCLOUD_TOKEN=

# Required when cloudflare_zone_id and domain_name are set in terraform.tfvars.
CLOUDFLARE_API_TOKEN=

# Used by ops/enable-spacetimedb-tls.sh.
LETSENCRYPT_EMAIL=
